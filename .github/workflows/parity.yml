name: Cross-host parity

# Phase 85 — behavioural cross-implementation parity gate.
#
# The same canonical wire JSON, rendered through the TS @fuaran-ui/renderer
# (ts-host.html) and the F# Fuaran.UI.Renderer (fable-host.html, Fable-compiled),
# must produce structurally identical DOM with the same CSS class vocabulary +
# ARIA contract for the primitive matrix. Any divergence FAILS THE BUILD — the
# regression guard over the Phase 77 reference-CSS byte-copy + parity-locked
# class names.
#
# Like conformance.yml, this gate assembles a multi-repo layout because the
# Fable host project-references the sibling F# tier (the @fuaran-ui/* npm
# packages resolve from the registry). Every checked-out repo is public —
# no token needed.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Least privilege, stated rather than inherited. This workflow reads the tree and
# writes nothing back — no package publish, no deployment, no PR comment.
permissions:
  contents: read

jobs:
  parity:
    runs-on: ubuntu-latest
    steps:
      # Assemble the workspace layout:
      #   <root>/wire-format-fixtures   (fuaran-ui-specification — the parity fixtures)
      #   <root>/fuaran                 (F# tier — Fable host project reference)
      #   <root>/fuaran-live            (this repo)
      - name: Checkout the wire-format corpus (fuaran-ui-specification)
        uses: actions/checkout@v4
        with:
          repository: fuaran-ui/fuaran-ui-specification
          path: workspace/wire-format-fixtures

      - name: Checkout fuaran (F# tier)
        uses: actions/checkout@v4
        with:
          # Pinned to an exact ref: the F# tier is a PROJECT REFERENCE, so a
          # floating default branch let any upstream push redden this repo with no
          # commit here. A RELEASE TAG: v0.12.0 post-dates the Phases 692-694
          # flattened NodeKind this repo authors against, so the pin is back on a
          # tag (see ci.yml for why it was a bare SHA in between).
          # Rationale + bump procedure in ci.yml. The corpus checkout stays unpinned
          # deliberately — it is the shared conformance gate.
          repository: fuaran-ui/fuaran-dotnet
          # The PLAYGROUND project no longer project-references this repo at
          # all: it consumes the language tier as published nuget.org packages at
          # its own pin. So this checkout serves the SHOWCASE project alone, and
          # this ref decides which tier contracts that project compiles against —
          # a pin in every sense the package pins are, spelled as a git ref.
          #
          # It was a bare commit SHA from Phase 761 until this bump, for a reason
          # that has now expired: the showcase had adopted contracts postdating
          # every released tag, so no tag could compile it, and the note here said
          # to return to a tag once the tier cut a release carrying them. v0.75.0
          # is that release.
          # Raised to v0.78.0 on 2026-09-08, with the package pins in
          # app/FuaranLive.fsproj and app/showcase/Showcase.fsproj: that release
          # widens Action.Navigate to a (TextSource, NavigateTarget) pair, adds a
          # required Node.visible, adds SwitchCase.when while making its match
          # optional, and adds RenderContext.CustomHashFloor -- every one of which
          # the showcase source now names. It also carries the Fable
          # dynamic-Huffman inflate fix the teleport receiver needs to accept a
          # bundle any standard deflater produced, and the print-break,
          # node-tooltip and upload-sink members.
          # Raised again to v0.78.1 the same day, and that one is not a widening: the
          # renderer's resume path named a wire type its Fable-only arm never opened,
          # so v0.78.0 built for .NET and failed to Fable-compile for every consumer
          # of it -- this repo among them, through both the packaged playground copy
          # and the project-referenced showcase copy. The showcase checks THIS tier
          # out, so the ref is what carries the fix here.
          # Raised to v0.79.0 on 2026-09-09 (Phase 1627), the tier's first deliberately
          # BREAKING release in some time: RenderContext gains a required Csp field, so
          # every full-literal construction of it in app/showcase/ stops compiling. That
          # is the source-affecting cost this comment warns about, arriving in full --
          # which is why the four `Csp = Csp.Permissive` assignments land in the SAME
          # commit as this ref. They cannot be split: the field assignment alone fails
          # against a sibling checked out at v0.78.1, where RenderContext has no such
          # field, and the ref alone fails once the field is required. Permissive is the
          # tier's own canonical default and emits byte-identical output, so the
          # showcase's rendered bytes do not move.
          #
          # A tag is immutable, so this is no less deliberate than the SHA it
          # replaces. Bumping it is a SOURCE-AFFECTING act rather than a
          # housekeeping one: a tier release that widens a record breaks every
          # full-literal construction of it in app/showcase/, which is what the
          # move off the SHA cost here.
          #
          # To bump: raise the ref in all six workflows together (grep the ref).
          ref: v0.79.0
          path: workspace/fuaran-dotnet

      - name: Checkout fuaran-live (this repo)
        uses: actions/checkout@v4
        with:
          path: workspace/fuaran-live

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup .NET (Fable host)
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      # NuGet hard-errors (NU1301) on the F# tier's missing local folder feed;
      # mint it empty — Fuaran.Core.* restores from nuget.org.
      - name: Create empty local-nuget-feed
        run: mkdir -p "$GITHUB_WORKSPACE/../local-nuget-feed"

      - name: Install fuaran-live
        working-directory: workspace/fuaran-live
        run: pnpm install --frozen-lockfile

      - name: Restore .NET tools (Fable)
        working-directory: workspace/fuaran-live
        run: dotnet tool restore

      - name: Install Playwright browser
        working-directory: workspace/fuaran-live
        run: pnpm exec playwright install --with-deps chromium

      - name: Build the dual-host artifact
        working-directory: workspace/fuaran-live
        run: pnpm run fable:app && pnpm run build:dual

      # Runs the Playwright parity gate against the built dist. Fails the build
      # on any cross-host DOM/CSS divergence.
      - name: Cross-host parity gate
        working-directory: workspace/fuaran-live
        run: pnpm run parity
